.worktrees/
.superpowers/
node_modules/
.turbo/
.next/
coverage/
playwright-report/
test-results/
.env
.env.local
.env.*.local
!.env.example
supabase/.temp/
dist/
apps/*/.next/

.vercel
.env*

# Third-party historical research corpus. Downloaded by
# `tooling/scripts/historical-download.mjs`, never committed: Football-Data.co.uk
# publishes no explicit redistribution licence, so the files stay local and only
# their provenance, checksums and derived model artifacts are versioned.
data/historical/
