/dist/
.vinext/
.wrangler/

# Generated, and rewritten differently by `next typegen` and `vinext build`.
# Tracking it guarantees a permanently dirty tree after a build. `typecheck`
# runs `typegen:next` first, so a fresh clone always regenerates it.
next-env.d.ts
